include_rules

TEMPLATE += -e "s:--PREFIX--:$(PREFIX):g"
TEMPLATE += -e "s:--PACKAGE--:$(PACKAGE):g"
TEMPLATE += -e "s:--VERSION--:$(CURRENT_VERSION):g"

: foreach *.in |> < %f sed $(TEMPLATE) > %o && chmod +x %o |> %B.sh