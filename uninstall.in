#!/bin/sh

if [ `id -u` != 0 ]; then
  echo "Error: you must be root to install!"
  exit 1
fi

rm -f @PREFIX@/lib/libgtkkit.dylib
rm -rf @PREFIX@/share/doc/GTKKit
rm -rf @PREFIX@/include/GTKKit
rm -f @PREFIX@/share/pkgconfig/libgtkkit.pc